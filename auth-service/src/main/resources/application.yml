spring:
 application:
  name: auth-service
 profiles:
  active: dev
 security:
  oauth2:
   authorizationserver:
    issuer: http://localhost:9003

server:
 port: 9003
 servlet:
  context-path: /Auth

eureka:
 client:
  service-url:
   defaultZone: http://localhost:9001/eureka/
   
