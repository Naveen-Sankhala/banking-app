spring:
 application:
  name: auth-service
 profiles:
  active: dev
 security:
  oauth2:
   authorizationserver:
    issuer: http://localhost:9003
   client-id: bank-web
   client-secret: secret
   redirect-uri:
   authorization-uri: /oauth2/authorize
   get.token-uri: /token
   refresh.token-uri: /refresh
   logout.token-uri: /logout
   token-expiration: 120
   refreshToken-expiration: 180
   read-scope: bank.read
   write-scope: bank.write
   openid-scope: openid
   profile-scope: profile
    

server:
 port: 9003
 servlet:
  context-path: /Auth

#eureka:
# client:
#  service-url:
#   defaultZone: http://localhost:9001/eureka/
#   

corss:
 url: "*" 
#================================
#JWT Setting
#================================
#
#oauth2:
# signing.key.secret: bank-web
# get.token.uri: /oauth2/token
# authorize.token.uri: /oauth2/authorize
# logout.token.uri: /logout
# refresh.token.uri: /refresh
# http.request.header: Authorization
# token.expiration.in.seconds: 120
# refreshToken.expiration.in.seconds: 180

   
